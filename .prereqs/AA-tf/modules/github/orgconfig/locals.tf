locals {
  my_gh_network_config_name = "${var.workload_nickname}nc"
  my_gh_runner_group_name = "${var.workload_nickname}rngp"
  my_gh_runner_name = "${var.workload_nickname}rn"
  my_gh_repo_name = "${var.workload_nickname}repo"
}
